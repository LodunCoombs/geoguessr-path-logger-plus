Fixed issue with script recording path while watching a replay.

Now gets duel game id from websocket instead of HTTP requests (which weren't working).

When you change settings, triggers a re-render of the paths on screen with the new settings.
